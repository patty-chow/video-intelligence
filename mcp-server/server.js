/**
 * MCP Server for Video Intelligence Pipeline
 *
 * STATUS: Stub — not yet implemented.
 *
 * This will eventually expose the pipeline as MCP tools that Claude Code
 * can call conversationally. For example:
 *
 *   "Analyze the latest video I downloaded and tell me what hook pattern it uses"
 *   "Run a trend report on all the videos in my TikTok saves folder"
 *   "Write me a script that uses the curiosity gap hook from that fitness video"
 *
 * The MCP server would register tools like:
 *   - analyze_video(path) → returns structured analysis
 *   - generate_script(analysis, niche) → returns a script
 *   - generate_breakdown(analysis) → returns a breakdown
 *   - batch_analyze(directory, niche) → returns a trend report
 *   - list_analyses() → returns past analyses from output/
 *
 * To build this out, check the MCP SDK:
 *   https://modelcontextprotocol.io/introduction
 *
 * Install: npm install @modelcontextprotocol/sdk
 */

console.log("MCP server not yet implemented.");
console.log("Get the core pipeline working first (see README), then come back here.");
console.log("");
console.log("To get started with MCP:");
console.log("  npm install @modelcontextprotocol/sdk");
console.log("  See: https://modelcontextprotocol.io/introduction");
